spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#management:
#  endpoint:
#    bus-refresh:
#      enabled: true

